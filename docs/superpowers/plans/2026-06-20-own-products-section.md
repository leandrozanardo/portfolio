# Own Products Section Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Add premium own-products showcase section with NEXION CORE featured and SprintDeck secondary.

**Architecture:** Data in `ownProducts.ts`, copy in i18n, `ProductShowcaseCard` + `OwnProductsSection`, integrated in HomePage after FeaturedCaseStudies.

**Tech Stack:** React 18, TypeScript, Tailwind, i18next, Vitest

---

See parent plan for BDD scenarios and task breakdown. Implementation follows TDD in `App.test.tsx`.
